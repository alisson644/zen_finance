<p style="color: green"><%= notice %></p>

<h1>Categories</h1>

<div id="sources">
  <% @categories.each do |category| %>
    <%= render category %>
    <p>
      <%= link_to "Show this category", category %>
    </p>
  <% end %>
</div>

<%= link_to "New source", new_category_path %>
